- errores:
	* farms-services, delete, <granja-gslb>    no muestra descripcion
	* farms-services, --, <granja l4>		arreglar el error
	* cuando no hay objectos. p.e. borrando cert de l4: "The farms-certificates remove l4 command has no proc or method to call!"

- crear paquete

- añadir opciones.
	- modo script

- elegir el formato de los parametros

- devolver error si hay parametros en el comando cuando es GET.

- doc

- AÑADIR MODULOS:
	graficas?

- completar funcionalidades:
	farms-add/rem headers
	farms-zones-resources
	farms-services MOVE
	farms-certificates MOVE
	rbac-roles SET		# puede dar problemas por como se parsean los parametros
	# configurar scheduler de tiempo
	# asignar y desasignar recursos de un grupo RBAC
	# license. Debe tratarse el recibir datos en txt

- copy:  (Completar buscando parametro copy_from)
	fg

- hacer que si falla parseando, se pueda dar al autocompletado, parseando y añadiendo los parametros a la estructura cmd y recargandola

- añadir etiqueta de ERRROR o SUCCESS
	- cambiar color el prompt?

- mejorar descripcion de error.
# system-backups upload
The file name to upload is not set
	- Añadiendo como seria el comando esperado:
	system-backups upload <file>

- errores
	subiendo tar.gz
	IPDS: bl-sources, usar "UriParamTag"
	bonding-slaves, usar "UriParamTag"
